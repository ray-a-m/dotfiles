# Kitty terminal config

# Font
font_family Departure Mono
font_size 11.0

# Tab bar — kitty draws nested rounded powerline caps natively, so the
# font does not need to be patched with Nerd Font glyphs.
tab_bar_edge top
tab_bar_style powerline
tab_powerline_style round
active_tab_font_style bold

# Tab navigation
map cmd+1 previous_tab
map cmd+3 next_tab

# Theme tracks Omarchy's active theme — re-includes on kitty restart, which
# omarchy-restart-terminal triggers as part of omarchy-theme-set.
include ~/.config/omarchy/current/theme/kitty.conf
