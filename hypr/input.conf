# Control your input devices
# See https://wiki.hypr.land/Configuring/Basics/Variables/#input
input {
  # Use multiple keyboard layouts and switch between them with Left Alt + Right Alt
  # kb_layout = us,dk,eu

  # Use a specific keyboard variant if needed (e.g. intl for international keyboards)
  # kb_variant = intl

  kb_layout = us
  # No global caps/ctrl remap.  The laptop keyboard is remapped by keyd
  # (dotfiles/keyd/default.conf); the Corne is configured in Vial.  The empty
  # value overrides omarchy's default compose:caps -- caps stays caps here.
  kb_options =

  # Change speed of keyboard repeat
  repeat_rate = 40
  repeat_delay = 250

  # Start with numlock on by default
  numlock_by_default = true

  # Increase sensitivity for mouse/trackpad (default: 0)
  # sensitivity = 0.35

  # Turn off mouse acceleration (default: adaptive)
  accel_profile = flat

  touchpad {
    # Use natural (inverse) scrolling
    natural_scroll = true

    # Use two-finger clicks for right-click instead of lower-right corner
    clickfinger_behavior = true

    # Touchpad scroll_factor is set per-machine in ~/.config/hypr/local.conf
    # (touchpad sensitivity varies enough between trackpads to not pin it here).

    # Enable the touchpad while typing
    # disable_while_typing = false

    # Left-click-and-drag with three fingers
    # drag_3fg = 1
  }
}

# Enable touchpad gestures for changing workspaces
# See https://wiki.hypr.land/Configuring/Advanced-and-Cool/Gestures/
# gesture = 3, horizontal, workspace

# Enable touchpad gestures for moving focus (helpful on scrolling layout)
# gesture = 3, left,  dispatcher, movefocus, l
# gesture = 3, right, dispatcher, movefocus, r

# The internal laptop keyboard's remaps (caps->alt, right-alt->ctrl, and the
# left-Alt/left-Super bottom-row swap) live in keyd now -- dotfiles/keyd/ --
# because caps->alt and ralt->ctrl aren't expressible in XKB.  keyd grabs the
# device, so a hypr per-device block keyed on its name would no longer match.
